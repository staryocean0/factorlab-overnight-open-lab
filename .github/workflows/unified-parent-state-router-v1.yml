name: Unified parent-state router v1

on:
  push:
    branches:
      - codex/unified-parent-state-router-20260908
    paths:
      - '.github/workflows/unified-parent-state-router-v1.yml'
      - 'scripts/run_rmr_unified_parent_state_router_v1.py'
      - 'scripts/certify_rmr_unified_parent_state_router_v1_blackbox.py'
      - 'tests/test_rmr_unified_parent_state_router_v1.py'
      - 'tests/test_reusable_three_role_blackbox.py'
      - 'docs/governance/reversal_mean_reversion_unified_parent_state_router_v1_protocol.json'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: python -m pip install --upgrade pip pandas numpy pyarrow scikit-learn pytest
      - name: Test frozen unified router
        run: python -m pytest -q tests/test_rmr_unified_parent_state_router_v1.py tests/test_reusable_three_role_blackbox.py
      - name: Run detailed unified router DEV VALIDATION
        run: |
          python scripts/run_rmr_unified_parent_state_router_v1.py \
            --receipt /tmp/router_validation.json \
            --freeze /tmp/router_freeze.json
      - name: Run low-bandwidth unified router BLACKBOX certification
        shell: bash
        run: |
          set +e
          python scripts/certify_rmr_unified_parent_state_router_v1_blackbox.py \
            --freeze /tmp/router_freeze.json \
            --receipt /tmp/router_blackbox.json
          code=$?
          set -e
          if [ "$code" -ne 0 ] && [ "$code" -ne 2 ] && [ "$code" -ne 3 ]; then
            echo "unified router BLACKBOX infrastructure failure"
            exit "$code"
          fi
      - name: Enforce BLACKBOX low bandwidth
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          p=json.loads(Path('/tmp/router_blackbox.json').read_text())
          allowed={
            'schema_id','research_identity','query_id','candidate_fingerprint',
            'protocol_fingerprint','blackbox_source_fingerprint','blackbox_window',
            'decision','details_released','exact_metrics_released','counts_released',
            'subperiods_released','event_rows_released','blackbox_remains_closed',
            'repeated_query_is_independent_OOS','production_authority'
          }
          assert set(p)==allowed
          assert p['decision'] in {'PASS','FAIL','INSUFFICIENT'}
          assert p['details_released'] is False
          assert p['exact_metrics_released'] is False
          assert p['counts_released'] is False
          assert p['subperiods_released'] is False
          assert p['event_rows_released'] is False
          print(p['decision'])
          PY
      - name: Upload bounded outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unified-parent-state-router-v1
          path: |
            /tmp/router_validation.json
            /tmp/router_freeze.json
            /tmp/router_blackbox.json
          if-no-files-found: warn
