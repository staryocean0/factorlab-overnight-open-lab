# Archive

This directory contains closed or completed Overnight research entrypoints that remain useful for provenance but must not be treated as active execution surfaces.

Current active authority lives in:

- `docs/governance/current_authority_v1.json`
- `docs/governance/overnight_factor_product_registry_v1.json`

Rules:

- archived files preserve historical bytes and reasoning;
- do not execute archived scripts or handoffs unless a new result-free protocol explicitly revives the underlying idea;
- moving a file here does not erase its historical authority or Git provenance;
- active work must be linked from `docs/governance/current_authority_v1.json`.

Current archived groups:

- `v6a_short0935_to_close_20260910/` — closed before DEV because the 09:35-to-close objective was outside the Overnight model's causal scope;
- `v6a_reusable_blackbox_completed_20260910/` — completed one-command V6A BLACKBOX execution entrypoint and local handoff; the reusable controller library remains active because current factor research imports its frozen feature/source functions.
