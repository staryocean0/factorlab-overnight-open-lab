# Gap-Fill V2.1 DEV cloud adjudication — 2026-09-08

Research identity: `gap_fill_v2_1_regime_conditioned_successor`.

Decision: **`V21_DEV_no_P2_successor`**.

## Receipt reviewed

Local result commit:

`c34c74809d19211124c4e66f1afb29ade86b6cb3`

It adds only:

- `docs/research/local_gap_fill_v21_dev_selection_receipt_v1.json`;
- `docs/governance/local_gap_fill_v21_dev_data_usage_v1.json`.

No selected parameter freeze was written.

The execution preserved the frozen V21 runner / P2 math / evaluation kernel / candidate family / selection protocol despite the concurrent repository-wide authority reset. The reset therefore does not invalidate this bounded R4 specialist execution.

## Source and boundary integrity

The DEV metadata preflight reproduced the admitted SA2 inventory for both CSI300 and CSI500:

- observed trading days: `975` each;
- session-complete-239 days: `944` each;
- incomplete days: `31` each;
- optional `14:59` present days: `884` each.

Parent HE-00 v8 identity remained:

`25f4f9f8b67c799ffb1a7b7fdee94b0b21dbbfed1efc54d063264ccb046411f0`.

The run kept Audit A, Audit B, the external reserve, 2014Q4 supporting crosscheck and CSI1000 post-2026-08-21 outcomes sealed. No feature/model/hyperparameter/threshold/calibration search or trading-return selection was performed.

## Frozen ladder result

Exactly the three preregistered candidates were attempted in order:

1. `P2_XI_SHARED_1B`;
2. `P2_CSI500_SHARED_1B`;
3. `P2_CSI500_STAGE_2B`.

`selected_candidate = null`.

The decisive gate was the frozen DEV primary sample-sufficiency rule for CSI500-high `abs_gap > 10bp`:

- 2016: `68`;
- 2017: **`15`**;
- 2018: `52`;
- pooled: `135`.

The contract required at least `20` rows in **each** validation year and at least `60` pooled. The pooled count passes, but 2017 fails. Therefore the primary evaluation status is `evidence_insufficient` and no P2 candidate can become eligible.

Descriptive beta histories do not override that failure:

- cross-index shared: `[-0.06920, -0.02054, +0.06877]`;
- CSI500 shared: `[+0.09194, -0.02862, -0.00640]`;
- CSI500 stage-specific 15m: `[+0.33435, +0.01736, -0.00064]`;
- CSI500 stage-specific 60m: `[-0.08631, -0.05906, -0.01043]`.

## Closeout rule

The bounded P2 family is closed. The following are forbidden rescue actions under this identity:

- extend or slide DEV dates;
- lower the per-year minimum;
- change the `>10bp` primary cohort;
- add/reorder candidates;
- promote the same-era geometry control as a V2.1 mechanism successor;
- open Audit A to see whether the family would work there.

Any return to the relative-gap-excess mechanism requires a new identity, genuinely new evidence and a new results-blind protocol.

## Authority after closeout

- V21 P2 family: **closed — no successor**;
- V21 DEV: consumed development evidence;
- Audit A/B and external reserve: sealed;
- production authority: false.

Repository-wide work continues under the broad reversal / mean-reversion direction-finder. V21 remains a closed R4 specialist case study and does not block R1/R5 work.
